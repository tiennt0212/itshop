import { BrowserRouter, Route, Switch } from 'react-router-dom';
import { ROUTES } from './utils/routeConstants';

const Routes = () => {
    return (
        <BrowserRouter>
        <Switch>
            
        </Switch>
        </BrowserRouter>
    )
}